{
  "actions": [{
    "@type": "type.googleapis.com/WriteGlobalVar",
    "varName": "分词结果",
    "op": "WriteGlobalVarOp_Override",
    "customContextDataKey": {
    },
    "id": "A-50525cb2-43c8-4d7a-960a-d1aa053e062e"
  }, {
    "@type": "type.googleapis.com/ShellCommand",
    "command": "#!/system/bin/sh\n\nBASE_DIR\u003d\"/data/system\"\nMATCH\u003d$(ls \"$BASE_DIR\" | grep \u0027^shortx_\u0027 | head -n 1)\n\nif [ -z \"$MATCH\" ]; then\n  echo \"未找到 ShortX 目录\"\n  exit 1\nfi\n\nCLIP_FILE\u003d\"$BASE_DIR/$MATCH/ShortX/剪切板/clipboard_data.dat\"\necho \"$CLIP_FILE\"",
    "customContextDataKey": {
      "keys": [{
        "first": "shellOut",
        "second": "clip"
      }]
    },
    "isDisabled": true,
    "note": "适合本地剪贴板文件",
    "id": "A-63876334-039a-46c5-b715-51642f5c9c8b"
  }, {
    "@type": "type.googleapis.com/ShellCommand",
    "command": "#!/system/bin/sh\n\nBASE_DIR\u003d\"/data/system\"\nMATCH\u003d$(ls \"$BASE_DIR\" | grep \u0027^shortx_\u0027)\nCLIP_DIR\u003d\"$BASE_DIR/$MATCH/ShortX/全局剪贴板\"\n\nDATE_DIR\u003d$(date +%Y年%m月%d日)\nDATE_FILE\u003d$(date +%Y%m%d)\n\necho \"$CLIP_DIR/$DATE_DIR/剪贴板_${DATE_FILE}.md\"",
    "customContextDataKey": {
    },
    "note": "配合全局剪贴板",
    "id": "A-b56c538f-54c0-4735-afbb-4147d05afbad"
  }, {
    "@type": "type.googleapis.com/ShowTextFieldDialog",
    "cancelable": true,
    "textFields": [{
      "placeholder": "输入"
    }],
    "style": {
    },
    "customContextDataKey": {
    },
    "id": "A-6db97b3d-806f-4458-84dc-ee5a2aa7f7d4"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(java.io);\nimportClass(android.graphics.PixelFormat);\nimportClass(android.os.Handler);\nimportClass(android.os.Looper);\nimportClass(java.lang.Runnable);\nimportClass(android.graphics.Color);\nimportClass(android.graphics.drawable.GradientDrawable);\n\n// 文件路径\nvar filePath \u003d String(shellOut || \"\").trim();\n\n// 搜索函数\nfunction searchClipboardLines(searchText) {\n  try {\n    var file \u003d new File(filePath);\n    if (!file.exists()) return [\"文件不存在\"];\n    var reader \u003d new BufferedReader(new InputStreamReader(new FileInputStream(file), \"UTF-8\"));\n    var line, results \u003d [];\n    while ((line \u003d reader.readLine()) !\u003d null) {\n      if (!line.includes(\"来自:\") \u0026\u0026 line.includes(searchText)) {\n        results.push(line);\n      }\n    }\n    reader.close();\n    return results.length \u003e 0 ? results : [\"未找到匹配内容\"];\n  } catch (e) {\n    return [\"搜索失败: \" + e.toString()];\n  }\n}\n\n// 圆角背景\nfunction getRoundedDrawable(color, radius) {\n  var drawable \u003d new GradientDrawable();\n  drawable.setShape(GradientDrawable.RECTANGLE);\n  drawable.setCornerRadius(radius);\n  drawable.setColor(color);\n  return drawable;\n}\n\n// 创建浮窗\nnew Handler(Looper.getMainLooper()).post(new Runnable({\n  run: function () {\n    try {\n      if (!context) return;\n      var wm \u003d context.getSystemService(Context.WINDOW_SERVICE);\n      if (!wm) return;\n\n      var layout \u003d new LinearLayout(context);  \n      layout.setOrientation(LinearLayout.VERTICAL);  \n      layout.setPadding(20, 20, 20, 20);  \n\n      var outerLayout \u003d new LinearLayout(context);  \n      outerLayout.setOrientation(LinearLayout.VERTICAL);  \n      outerLayout.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#E6EDF5\"), 25));  \n      outerLayout.setPadding(20, 20, 20, 20);  \n\n      // 顶部关闭图标  \n      var topBar \u003d new LinearLayout(context);  \n      topBar.setOrientation(LinearLayout.HORIZONTAL);  \n      topBar.setGravity(Gravity.RIGHT);  \n      var closeBtn \u003d new TextView(context);  \n      closeBtn.setText(\"✕\");  \n      closeBtn.setTextSize(18);  \n      closeBtn.setTextColor(Color.parseColor(\"#1E1E1E\"));  \n      closeBtn.setPadding(20, 10, 20, 10);  \n      closeBtn.setGravity(Gravity.CENTER);  \n      closeBtn.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#B0C8D8\"), 30));  \n      closeBtn.setOnClickListener(new View.OnClickListener({  \n        onClick: function () { wm.removeView(layout); }  \n      }));  \n      topBar.addView(closeBtn);  \n      outerLayout.addView(topBar);  \n\n      // 输入框  \n      var input \u003d new EditText(context);  \n      input.setHint(\"输入关键词\");  \n      input.setTextColor(Color.parseColor(\"#1E1E1E\"));  \n      input.setHintTextColor(Color.parseColor(\"#5D6B72\"));  \n      input.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#D0E3ED\"), 20));  \n      input.setTextSize(14);  \n      input.setPadding(30, 20, 30, 20);  \n      outerLayout.addView(input);  \n\n      // 滚动区  \n      var resultLayout \u003d new LinearLayout(context);  \n      resultLayout.setOrientation(LinearLayout.VERTICAL);  \n      resultLayout.setPadding(10, 10, 10, 10);  \n\n      var scrollView \u003d new ScrollView(context);  \n      scrollView.addView(resultLayout);  \n      outerLayout.addView(scrollView);  \n\n      // 复制函数  \n      function copyText(text) {  \n        try {  \n          var clipboard \u003d context.getSystemService(Context.CLIPBOARD_SERVICE);  \n          var clip \u003d android.content.ClipData.newPlainText(\"Copied Text\", text);  \n          clipboard.setPrimaryClip(clip);  \n          toast(\"已复制\");  \n          wm.removeView(layout);  \n        } catch (e) {  \n          toast(\"复制失败\");  \n        }  \n      }  \n\n      // 渲染结果  \n      function showResults(lines) {  \n        resultLayout.removeAllViews();  \n        for (var i \u003d 0; i \u003c lines.length; i++) {  \n          var lineText \u003d lines[i];  \n\n          var lineLayout \u003d new LinearLayout(context);  \n          lineLayout.setOrientation(LinearLayout.HORIZONTAL);  \n          lineLayout.setPadding(0, 10, 0, 10);  \n\n          // 创建一个水平滚动视图\n          var horizontalScrollView \u003d new HorizontalScrollView(context);\n          var scrollLayout \u003d new LinearLayout(context);\n          scrollLayout.setOrientation(LinearLayout.HORIZONTAL);\n          horizontalScrollView.addView(scrollLayout);\n\n          // 复制按钮\n          var copyBtn \u003d new TextView(context);\n          copyBtn.setText(\"⎘\");\n          copyBtn.setTextSize(16);\n          copyBtn.setTextColor(Color.WHITE);\n          copyBtn.setPadding(20, 10, 20, 10);\n          copyBtn.setGravity(Gravity.CENTER);\n          copyBtn.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#7BAEDA\"), 20));\n          copyBtn.setOnClickListener(new View.OnClickListener({\n            onClick: (function (t) {\n              return function () { copyText(t); };\n            })(lineText)\n          }));\n          lineLayout.addView(copyBtn);\n\n          // 剪切按钮\n          var cutBtn \u003d new TextView(context);\n          cutBtn.setText(\"✂\");\n          cutBtn.setTextSize(16);\n          cutBtn.setTextColor(Color.WHITE);\n          cutBtn.setPadding(20, 10, 20, 10);\n          cutBtn.setGravity(Gravity.CENTER);\n          cutBtn.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#FF8C42\"), 20));\n          cutBtn.setOnClickListener(new View.OnClickListener({\n            onClick: (function (t) {\n              return function () {\n                try {\n                  var tokens \u003d t.split(\" \").join(\" / \");\n                  shortx.writeGlobalVarWithOp(\"分词结果\", tokens, 3);\n                  toast(\"全局变量已写入\");\n                } catch (e) {\n                  toast(\"分词失败: \" + e);\n                }\n                wm.removeView(layout);\n              };\n            })(lineText)\n          }));\n          lineLayout.addView(cutBtn);\n\n          // 将文本视图放入横向滚动布局中\n          var textView \u003d new TextView(context);\n          textView.setTextColor(Color.parseColor(\"#1E1E1E\"));\n          textView.setTextSize(14);\n          textView.setText(lineText);\n          textView.setLayoutParams(new LinearLayout.LayoutParams(\n            LinearLayout.LayoutParams.WRAP_CONTENT, LinearLayout.LayoutParams.WRAP_CONTENT\n          ));\n          scrollLayout.addView(textView);\n\n          // 将滚动视图加入到行布局中\n          lineLayout.addView(horizontalScrollView);\n\n          resultLayout.addView(lineLayout);  \n        }  \n      }  \n\n      // 初始内容  \n      var searchText \u003d String(textFieldInput1 || \"\").trim();  \n      input.setText(searchText);  \n      showResults(searchClipboardLines(searchText));  \n\n      // 实时监听输入变化  \n      input.addTextChangedListener(new android.text.TextWatcher({  \n        afterTextChanged: function (s) {  \n          showResults(searchClipboardLines(s.toString()));  \n        },  \n        beforeTextChanged: function () {},  \n        onTextChanged: function () {}  \n      }));  \n\n      layout.addView(outerLayout);  \n\n      var params \u003d new WindowManager.LayoutParams(  \n        WindowManager.LayoutParams.MATCH_PARENT,  \n        WindowManager.LayoutParams.WRAP_CONTENT,  \n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,  \n        WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE,  \n        PixelFormat.TRANSLUCENT  \n      );  \n      params.gravity \u003d Gravity.TOP | Gravity.LEFT;  \n      params.x \u003d 100;  \n      params.y \u003d 300;  \n      wm.addView(layout, params);  \n    } catch (e) {  \n      toast(\"异常: \" + e.toString());  \n    }\n  }\n}));\n\n// Toast 函数\nfunction toast(msg) {\n  try {\n    android.widget.Toast.makeText(context, msg, android.widget.Toast.LENGTH_SHORT).show();\n  } catch (e) {}\n}",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "note": "配合全局剪贴板",
    "id": "A-c334e970-126c-4764-9267-c8fbe5b174e9"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(java.io);\nimportClass(android.graphics.PixelFormat);\nimportClass(android.os.Handler);\nimportClass(android.os.Looper);\nimportClass(java.lang.Runnable);\nimportClass(android.graphics.Color);\nimportClass(android.graphics.drawable.GradientDrawable);\nimportPackage(java.nio.charset);\n\nvar filePath \u003d String(clip || \"\").trim();\n\n// 预处理文本内容\nfunction preprocessText(text) {\n    // 1. 删除所有非中文、英文、数字的字符\n    text \u003d text.replace(/[^a-zA-Z0-9\\u4e00-\\u9fa5\\s]/g, \"\");\n\n    // 2. 在中文和英文之间添加空格\n    text \u003d text.replace(/([\\u4e00-\\u9fa5])([a-zA-Z0-9])/g, \"$1 $2\");\n    text \u003d text.replace(/([a-zA-Z0-9])([\\u4e00-\\u9fa5])/g, \"$1 $2\");\n\n    // 3. 清理多余的空格\n    text \u003d text.replace(/\\s+/g, \" \").trim();\n\n    return text;\n}\n\nfunction extractTextFromJson(jsonObj) {\n  if (!jsonObj) return null;\n  try {\n    if (jsonObj.content) {\n      var contentArray \u003d JSON.parse(jsonObj.content);\n      if (Array.isArray(contentArray) \u0026\u0026 contentArray.length \u003e 0 \u0026\u0026 contentArray[0].text) {\n        return preprocessText(contentArray[0].text); // 预处理文本\n      }\n    }\n  } catch (e) {}\n  try {\n    if (jsonObj.determineContent) {\n      var determineArray \u003d JSON.parse(jsonObj.determineContent);\n      if (Array.isArray(determineArray) \u0026\u0026 determineArray.length \u003e 0 \u0026\u0026 determineArray[0].text) {\n        return preprocessText(determineArray[0].text); // 预处理文本\n      }\n    }\n  } catch (e) {}\n  return null;\n}\n\nfunction searchClipboardLines(searchText) {\n  if (!searchText) return [\"请输入搜索内容\"];\n  try {\n    var file \u003d new File(filePath);\n    if (!file.exists()) return [\"文件不存在: \" + filePath];\n    var fileContent \u003d new java.lang.String(java.nio.file.Files.readAllBytes(file.toPath()), \"UTF-8\");\n    var results \u003d [];\n    try {\n      var jsonArray \u003d JSON.parse(fileContent);\n      for (var i \u003d 0; i \u003c jsonArray.length; i++) {\n        var item \u003d jsonArray[i];\n        var textContent \u003d extractTextFromJson(item);\n        if (textContent \u0026\u0026 textContent.indexOf(searchText) !\u003d\u003d -1) {\n          results.push(textContent);\n        }\n      }\n    } catch (e) {\n      return [\"JSON解析失败: \" + e.toString()];\n    }\n    return results.length \u003e 0 ? results : [\"未找到匹配内容\"];\n  } catch (e) {\n    return [\"搜索失败: \" + e.toString()];\n  }\n}\n\nfunction getRoundedDrawable(color, radius) {\n  var drawable \u003d new GradientDrawable();\n  drawable.setShape(GradientDrawable.RECTANGLE);\n  drawable.setCornerRadius(radius);\n  drawable.setColor(color);\n  return drawable;\n}\n\nnew Handler(Looper.getMainLooper()).post(new Runnable({\n  run: function () {\n    try {\n      if (!context) return;\n      var wm \u003d context.getSystemService(Context.WINDOW_SERVICE);\n      if (!wm) return;\n\n      var layout \u003d new LinearLayout(context);\n      layout.setOrientation(LinearLayout.VERTICAL);\n      layout.setPadding(20, 20, 20, 20);\n\n      var outerLayout \u003d new LinearLayout(context);\n      outerLayout.setOrientation(LinearLayout.VERTICAL);\n      outerLayout.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#E6EDF5\"), 25));\n      outerLayout.setPadding(20, 20, 20, 20);\n\n      var topBar \u003d new LinearLayout(context);\n      topBar.setOrientation(LinearLayout.HORIZONTAL);\n      topBar.setGravity(Gravity.RIGHT);\n      var closeBtn \u003d new TextView(context);\n      closeBtn.setText(\"✕\");\n      closeBtn.setTextSize(18);\n      closeBtn.setTextColor(Color.parseColor(\"#1E1E1E\"));\n      closeBtn.setPadding(20, 10, 20, 10);\n      closeBtn.setGravity(Gravity.CENTER);\n      closeBtn.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#B0C8D8\"), 30));\n      closeBtn.setOnClickListener(new View.OnClickListener({\n        onClick: function () { wm.removeView(layout); }\n      }));\n      topBar.addView(closeBtn);\n      outerLayout.addView(topBar);\n\n      var input \u003d new EditText(context);\n      input.setHint(\"输入关键词\");\n      input.setTextColor(Color.parseColor(\"#1E1E1E\"));\n      input.setHintTextColor(Color.parseColor(\"#5D6B72\"));\n      input.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#D0E3ED\"), 20));\n      input.setTextSize(14);\n      input.setPadding(30, 20, 30, 20);\n      outerLayout.addView(input);\n\n      var resultLayout \u003d new LinearLayout(context);\n      resultLayout.setOrientation(LinearLayout.VERTICAL);\n      resultLayout.setPadding(10, 10, 10, 10);\n\n      var scrollView \u003d new ScrollView(context);\n      scrollView.addView(resultLayout);\n      outerLayout.addView(scrollView);\n\n      function copyText(text) {\n        try {\n          var clipboard \u003d context.getSystemService(Context.CLIPBOARD_SERVICE);\n          var clip \u003d android.content.ClipData.newPlainText(\"Copied Text\", text);\n          clipboard.setPrimaryClip(clip);\n          toast(\"已复制\");\n          wm.removeView(layout);\n        } catch (e) {\n          toast(\"复制失败\");\n        }\n      }\n\n      function showResults(lines) {\n        resultLayout.removeAllViews();\n        for (var i \u003d 0; i \u003c lines.length; i++) {\n          var lineText \u003d lines[i];\n\n          // 判断文本长度，超长则截断\n          var maxLength \u003d 100; // 设置最大显示字符数\n          var truncatedText \u003d lineText.length \u003e maxLength ? lineText.substring(0, maxLength) + \"...\" : lineText;\n\n          // 创建横向滚动视图\n          var horizontalScrollView \u003d new HorizontalScrollView(context);\n          horizontalScrollView.setLayoutParams(new LinearLayout.LayoutParams(\n            LinearLayout.LayoutParams.MATCH_PARENT,\n            LinearLayout.LayoutParams.WRAP_CONTENT\n          ));\n\n          var lineLayout \u003d new LinearLayout(context);\n          lineLayout.setOrientation(LinearLayout.HORIZONTAL);\n          lineLayout.setPadding(0, 10, 0, 10);\n\n          // 添加复制按钮在前\n          var copyBtn \u003d new TextView(context);\n          copyBtn.setText(\"⎘\");\n          copyBtn.setTextSize(16);\n          copyBtn.setTextColor(Color.WHITE);\n          copyBtn.setPadding(20, 10, 20, 10);\n          copyBtn.setGravity(Gravity.CENTER);\n          copyBtn.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#7BAEDA\"), 20));\n          copyBtn.setOnClickListener(new View.OnClickListener({\n            onClick: (function (t) {\n              return function () { copyText(t); };\n            })(lineText)\n          }));\n          lineLayout.addView(copyBtn);\n\n          // 添加剪切按钮在前\n          var cutBtn \u003d new TextView(context);\n          cutBtn.setText(\"✂\");\n          cutBtn.setTextSize(16);\n          cutBtn.setTextColor(Color.WHITE);\n          cutBtn.setPadding(20, 10, 20, 10);\n          cutBtn.setGravity(Gravity.CENTER);\n          cutBtn.setBackgroundDrawable(getRoundedDrawable(Color.parseColor(\"#FF8C42\"), 20));\n          cutBtn.setOnClickListener(new View.OnClickListener({\n            onClick: (function (t) {\n              return function () {\n                try {\n                  var clipboard \u003d context.getSystemService(Context.CLIPBOARD_SERVICE);\n                  var clip \u003d android.content.ClipData.newPlainText(\"Copied Text\", t);\n                  clipboard.setPrimaryClip(clip);\n                } catch (e) {}\n\n                try {\n                  shortx.writeGlobalVarWithOp(\"分词结果\", t, 3);\n                  toast(\"已写入全局变量\");\n                } catch (e) {\n                  toast(\"分词失败\");\n                }\n\n                wm.removeView(layout);\n              };\n            })(lineText)\n          }));\n          lineLayout.addView(cutBtn);\n\n          // 添加文本\n          var textView \u003d new TextView(context);\n          textView.setTextColor(Color.parseColor(\"#1E1E1E\"));\n          textView.setTextSize(14);\n          textView.setText(truncatedText);\n          textView.setLayoutParams(new LinearLayout.LayoutParams(\n            LinearLayout.LayoutParams.WRAP_CONTENT,\n            LinearLayout.LayoutParams.WRAP_CONTENT\n          ));\n          lineLayout.addView(textView);\n\n          horizontalScrollView.addView(lineLayout);\n          resultLayout.addView(horizontalScrollView);\n        }\n      }\n\n      var searchText \u003d String(textFieldInput1 || \"\").trim();\n      input.setText(searchText);\n      showResults(searchClipboardLines(searchText));\n\n      input.addTextChangedListener(new android.text.TextWatcher({\n        afterTextChanged: function (s) {\n          showResults(searchClipboardLines(s.toString()));\n        },\n        beforeTextChanged: function () { },\n        onTextChanged: function () { }\n      }));\n\n      layout.addView(outerLayout);\n\n      var params \u003d new WindowManager.LayoutParams(\n        WindowManager.LayoutParams.MATCH_PARENT,\n        WindowManager.LayoutParams.WRAP_CONTENT,\n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n        WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE,\n        PixelFormat.TRANSLUCENT\n      );\n      params.gravity \u003d Gravity.TOP | Gravity.LEFT;\n      params.x \u003d 100;\n      params.y \u003d 300;\n      wm.addView(layout, params);\n    } catch (e) {\n      toast(\"异常: \" + e.toString());\n    }\n  }\n}));\n\nfunction toast(msg) {\n  try {\n    android.widget.Toast.makeText(context, msg, android.widget.Toast.LENGTH_SHORT).show();\n  } catch (e) {}\n}",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "isDisabled": true,
    "note": "适合本地剪贴板文件",
    "id": "A-2ab7faa3-8282-410e-b9b6-f6883e888687"
  }, {
    "@type": "type.googleapis.com/WaitUtilConditionMatch",
    "condition": [{
      "@type": "type.googleapis.com/EvaluateGlobalVar",
      "op": "IsNotEmpty",
      "varName": "分词结果",
      "payload": {
        "value": "globalVarOf$分词结果"
      },
      "customContextDataKey": {
      },
      "id": "C-79515c14-1c73-4246-8dac-7397c5d5988f"
    }],
    "timeoutStr": "0",
    "customContextDataKey": {
    },
    "id": "A-8e7a119d-bda4-43bf-965e-5ab3f5fa2a3f"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(android.graphics);\nimportPackage(java.lang);\n\nfunction runOnUiThread(fn) {\n    new Handler(Looper.getMainLooper()).post(new Runnable({ run: fn }));\n}\n\nrunOnUiThread(function() {\n    var windowManager \u003d context.getSystemService(Context.WINDOW_SERVICE);\n\n    var layout \u003d new LinearLayout(context);\n    layout.setOrientation(LinearLayout.VERTICAL);\n    layout.setBackgroundColor(Color.TRANSPARENT);\n\n    var backgroundDrawable \u003d new android.graphics.drawable.GradientDrawable();\n    backgroundDrawable.setColor(Color.WHITE);\n    backgroundDrawable.setCornerRadius(70);\n    layout.setBackground(backgroundDrawable);\n    layout.setPadding(40, 40, 40, 40);\n\n    var layoutParams \u003d new WindowManager.LayoutParams(\n        1400,\n        2000,\n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n        WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL | WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,\n        PixelFormat.TRANSLUCENT\n    );\n    layoutParams.gravity \u003d Gravity.CENTER;\n\n    var titleView \u003d new TextView(context);\n    titleView.setText(\"点击选词\");\n    titleView.setTextSize(24);\n    titleView.setTextColor(Color.BLACK);\n    titleView.setGravity(Gravity.CENTER);\n    titleView.setPadding(0, 0, 0, 30);\n    layout.addView(titleView);\n\n    var scrollView \u003d new ScrollView(context);\n    scrollView.setLayoutParams(new LinearLayout.LayoutParams(\n        LinearLayout.LayoutParams.MATCH_PARENT,\n        0, 1\n    ));\n    var wordContainer \u003d new LinearLayout(context);\n    wordContainer.setOrientation(LinearLayout.VERTICAL);\n    scrollView.addView(wordContainer);\n    layout.addView(scrollView);\n\n    var rangeToggleButton \u003d new Button(context);\n    rangeToggleButton.setText(\"范围模式：关闭\");\n    rangeToggleButton.setTextColor(Color.parseColor(\"#000000\"));\n    rangeToggleButton.setBackgroundColor(Color.parseColor(\"#E0E0E0\"));\n    layout.addView(rangeToggleButton);\n\n    var buttonLayout \u003d new LinearLayout(context);\n    buttonLayout.setOrientation(LinearLayout.HORIZONTAL);\n    buttonLayout.setGravity(Gravity.CENTER);\n    buttonLayout.setPadding(0, 20, 0, 0);\n    layout.addView(buttonLayout);\n\n    var confirmButton \u003d new Button(context);\n    confirmButton.setText(\"确定\");\n    confirmButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    confirmButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(confirmButton);\n\n    var cancelButton \u003d new Button(context);\n    cancelButton.setText(\"取消\");\n    cancelButton.setTextColor(Color.parseColor(\"#FF0000\"));\n    cancelButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(cancelButton);\n\n    const inputText \u003d globalVarOf$分词结果;\n    const words \u003d inputText ? inputText.match(/[\\u4e00-\\u9fa5]|[a-zA-Z0-9]+|[.,!?;@#]/g) : [];\n    var selectedIndices \u003d [];\n    var rangeMode \u003d false;\n    var rangeStart \u003d -1;\n\n    function getCharWidth(char) {\n        return /[\\u4e00-\\u9fa5]|[。！？；：]/.test(char) ? 2 : 1;\n    }\n\n    const maxCharsPerRow \u003d 37;\n    var currentRowChars \u003d 0;\n    var currentRowLayout \u003d new LinearLayout(context);\n    currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n    wordContainer.addView(currentRowLayout);\n\n    function getSelectedText() {\n        var selectedWords \u003d [];\n        var sortedIndices \u003d selectedIndices.slice().sort(function(a, b) { return a - b; });\n        sortedIndices.forEach(function(index, i) {\n            selectedWords.push(words[index]);\n        });\n        return selectedWords.reduce(function(result, word, i, arr) {\n            const isCurrentChinese \u003d /[\\u4e00-\\u9fa5]|[。！？；：]/.test(word);\n            const isNextChinese \u003d arr[i + 1] \u0026\u0026 /[\\u4e00-\\u9fa5]|[。！？；：]/.test(arr[i + 1]);\n            result +\u003d word;\n            if (!isCurrentChinese || !isNextChinese) result +\u003d \" \";\n            return result;\n        }, \"\").trim();\n    }\n\n    function toggleSelection(index, view) {\n        if (rangeMode) {\n            if (rangeStart \u003d\u003d\u003d -1) {\n                rangeStart \u003d index;\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            } else {\n                const rangeEnd \u003d index;\n                const min \u003d Math.min(rangeStart, rangeEnd);\n                const max \u003d Math.max(rangeStart, rangeEnd);\n                for (var i \u003d min; i \u003c\u003d max; i++) {\n                    if (!selectedIndices.includes(i)) {\n                        selectedIndices.push(i);\n                        wordElements[i].setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n                    }\n                }\n                rangeStart \u003d -1;\n            }\n        } else {\n            if (selectedIndices.indexOf(index) !\u003d\u003d -1) {\n                selectedIndices \u003d selectedIndices.filter(function(i) { return i !\u003d\u003d index; });\n                view.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n            } else {\n                selectedIndices.push(index);\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            }\n        }\n    }\n\n    var wordElements \u003d [];\n\n    words \u0026\u0026 words.forEach(function(word, index) {\n        const wordLength \u003d Array.from(word).reduce(function(sum, ch) {\n            return sum + getCharWidth(ch);\n        }, 0);\n\n        const totalLength \u003d currentRowChars + wordLength + (currentRowChars \u003e 0 ? 1 : 0);\n        if (totalLength \u003e maxCharsPerRow) {\n            currentRowChars \u003d 0;\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n        }\n\n        currentRowChars +\u003d wordLength + (currentRowChars \u003e 0 ? 1 : 0);\n\n        var wordElement \u003d new TextView(context);\n        wordElement.setText(word);\n        wordElement.setTextSize(20);\n        wordElement.setPadding(10, 10, 10, 10);\n        wordElement.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n        wordElement.setGravity(Gravity.CENTER);\n        wordElement.setOnClickListener(new android.view.View.OnClickListener({\n            onClick: function(view) {\n                toggleSelection(index, view);\n            }\n        }));\n\n        wordElements.push(wordElement);\n        currentRowLayout.addView(wordElement);\n    });\n\n    rangeToggleButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            rangeMode \u003d !rangeMode;\n            rangeStart \u003d -1;\n            rangeToggleButton.setText(\"范围模式：\" + (rangeMode ? \"开启\" : \"关闭\"));\n        }\n    }));\n\n    cancelButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    confirmButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var selectedText \u003d getSelectedText();\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(selectedText)\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    try {\n        windowManager.addView(layout, layoutParams);\n    } catch (e) {\n        console.log(\"添加视图失败：\" + e);\n    }\n});",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "id": "A-14c165bc-023b-40de-8186-041289c2b497"
  }],
  "id": "SHARED-DA-2c047aae-f771-4627-96d6-a5bd62a4849c",
  "lastUpdateTime": "1748761090648",
  "createTime": "1748175839824",
  "author": {
    "name": "ShortX"
  },
  "title": "剪贴板搜索  (Copy)2.0  (Copy)",
  "description": "最好配合群里的全局剪切板 v0.0.2 使用。如果想使用本地，必须去 lsp 下个模块 ClipboardList（可能只适合澎湃，或者你自己去找个能解码输入法剪贴板的软件），我是微信输入法，找到的文件在/data/data/com.tencent.wetype/files/clipboard_data.dat   自己把 dat 改成 txt 放到/data/system/shortx_LKXeqqrGoNxOBirB/ShortX/剪切板/clipboard_data.txt   没有就自己创。关于 内容 js 会过滤 json 格式，不需要更改了。加入了丐版分词",
  "versionCode": "1",
  "quit": {
  }
}
###------###
{"type":"da"}