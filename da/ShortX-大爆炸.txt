{
  "actions": [{
    "@type": "type.googleapis.com/GetTextFromScreenNode",
    "customContextDataKey": {
      "keys": [{
        "first": "textOfTheView",
        "second": "shellOut"
      }]
    },
    "id": "A-c620b17a-b1a2-480a-afbf-532b4d71c92a"
  }, {
    "@type": "type.googleapis.com/CreateLocalVar",
    "localVar": {
      "name": "selectedText",
      "type": {
        "@type": "type.googleapis.com/StringVar"
      }
    },
    "customContextDataKey": {
    },
    "id": "A-9cfb160a-1542-48bb-b975-90665855fb6d"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(android.graphics);\nimportPackage(java.lang);\n\nfunction runOnUiThread(fn) {\n    new Handler(Looper.getMainLooper()).post(new Runnable({ run: fn }));\n}\n\nrunOnUiThread(function() {\n    var windowManager \u003d context.getSystemService(Context.WINDOW_SERVICE);\n\n    var layout \u003d new LinearLayout(context);\n    layout.setOrientation(LinearLayout.VERTICAL);\n    layout.setBackgroundColor(Color.TRANSPARENT);\n\n    var backgroundDrawable \u003d new android.graphics.drawable.GradientDrawable();\n    backgroundDrawable.setColor(Color.WHITE);\n    backgroundDrawable.setCornerRadius(70);\n    layout.setBackground(backgroundDrawable);\n    layout.setPadding(40, 40, 40, 40);\n\n    var layoutParams \u003d new WindowManager.LayoutParams(\n        900,\n        1200,\n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n        WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL | WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,\n        PixelFormat.TRANSLUCENT\n    );\n    layoutParams.gravity \u003d Gravity.CENTER;\n\n    var titleView \u003d new TextView(context);\n    titleView.setText(\"点击选词\");\n    titleView.setTextSize(24);\n    titleView.setTextColor(Color.BLACK);\n    titleView.setGravity(Gravity.CENTER);\n    titleView.setPadding(0, 0, 0, 30);\n    layout.addView(titleView);\n\n    var scrollView \u003d new ScrollView(context);\n    scrollView.setLayoutParams(new LinearLayout.LayoutParams(\n        LinearLayout.LayoutParams.MATCH_PARENT,\n        0, 1\n    ));\n    var wordContainer \u003d new LinearLayout(context);\n    wordContainer.setOrientation(LinearLayout.VERTICAL);\n    scrollView.addView(wordContainer);\n    layout.addView(scrollView);\n\n    var rangeToggleButton \u003d new Button(context);\n    rangeToggleButton.setText(\"范围模式：关闭\");\n    rangeToggleButton.setTextColor(Color.parseColor(\"#000000\"));\n    rangeToggleButton.setBackgroundColor(Color.parseColor(\"#E0E0E0\"));\n    layout.addView(rangeToggleButton);\n\n    var buttonLayout \u003d new LinearLayout(context);\n    buttonLayout.setOrientation(LinearLayout.HORIZONTAL);\n    buttonLayout.setGravity(Gravity.CENTER);\n    buttonLayout.setPadding(0, 20, 0, 0);\n    layout.addView(buttonLayout);\n\n    var confirmButton \u003d new Button(context);\n    confirmButton.setText(\"确定\");\n    confirmButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    confirmButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(confirmButton);\n\n    var cancelButton \u003d new Button(context);\n    cancelButton.setText(\"取消\");\n    cancelButton.setTextColor(Color.parseColor(\"#FF0000\"));\n    cancelButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(cancelButton);\n\n    const inputText \u003d shellOut;\n    const words \u003d inputText ? inputText.match(/[\\u4e00-\\u9fa5]|[a-zA-Z0-9]+|[.,!?;@#]/g) : [];\n    var selectedIndices \u003d [];\n    var rangeMode \u003d false;\n    var rangeStart \u003d -1;\n\n    function getCharWidth(char) {\n        return /[\\u4e00-\\u9fa5]|[。！？；：]/.test(char) ? 2 : 1;\n    }\n\n    const maxCharsPerRow \u003d 37;\n    var currentRowChars \u003d 0;\n    var currentRowLayout \u003d new LinearLayout(context);\n    currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n    wordContainer.addView(currentRowLayout);\n\n    function getSelectedText() {\n        var selectedWords \u003d [];\n        var sortedIndices \u003d selectedIndices.slice().sort(function(a, b) { return a - b; });\n        sortedIndices.forEach(function(index, i) {\n            selectedWords.push(words[index]);\n        });\n        return selectedWords.reduce(function(result, word, i, arr) {\n            const isCurrentChinese \u003d /[\\u4e00-\\u9fa5]|[。！？；：]/.test(word);\n            const isNextChinese \u003d arr[i + 1] \u0026\u0026 /[\\u4e00-\\u9fa5]|[。！？；：]/.test(arr[i + 1]);\n            result +\u003d word;\n            if (!isCurrentChinese || !isNextChinese) result +\u003d \" \";\n            return result;\n        }, \"\").trim();\n    }\n\n    function toggleSelection(index, view) {\n        if (rangeMode) {\n            if (rangeStart \u003d\u003d\u003d -1) {\n                rangeStart \u003d index;\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            } else {\n                const rangeEnd \u003d index;\n                const min \u003d Math.min(rangeStart, rangeEnd);\n                const max \u003d Math.max(rangeStart, rangeEnd);\n                for (var i \u003d min; i \u003c\u003d max; i++) {\n                    if (!selectedIndices.includes(i)) {\n                        selectedIndices.push(i);\n                        wordElements[i].setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n                    }\n                }\n                rangeStart \u003d -1;\n            }\n        } else {\n            if (selectedIndices.indexOf(index) !\u003d\u003d -1) {\n                selectedIndices \u003d selectedIndices.filter(function(i) { return i !\u003d\u003d index; });\n                view.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n            } else {\n                selectedIndices.push(index);\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            }\n        }\n    }\n\n    var wordElements \u003d [];\n\n    words \u0026\u0026 words.forEach(function(word, index) {\n        const wordLength \u003d Array.from(word).reduce(function(sum, ch) {\n            return sum + getCharWidth(ch);\n        }, 0);\n\n        const totalLength \u003d currentRowChars + wordLength + (currentRowChars \u003e 0 ? 1 : 0);\n        if (totalLength \u003e maxCharsPerRow) {\n            currentRowChars \u003d 0;\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n        }\n\n        currentRowChars +\u003d wordLength + (currentRowChars \u003e 0 ? 1 : 0);\n\n        var wordElement \u003d new TextView(context);\n        wordElement.setText(word);\n        wordElement.setTextSize(20);\n        wordElement.setPadding(10, 10, 10, 10);\n        wordElement.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n        wordElement.setGravity(Gravity.CENTER);\n        wordElement.setOnClickListener(new android.view.View.OnClickListener({\n            onClick: function(view) {\n                toggleSelection(index, view);\n            }\n        }));\n\n        wordElements.push(wordElement);\n        currentRowLayout.addView(wordElement);\n    });\n\n    rangeToggleButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            rangeMode \u003d !rangeMode;\n            rangeStart \u003d -1;\n            rangeToggleButton.setText(\"范围模式：\" + (rangeMode ? \"开启\" : \"关闭\"));\n        }\n    }));\n\n    cancelButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\"isclickcancelbutton\")\n                .build();\n            shortx.executeAction(writeLocalVar);\n            \n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    confirmButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var selectedText \u003d getSelectedText();\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(selectedText)\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    try {\n        windowManager.addView(layout, layoutParams);\n    } catch (e) {\n        console.log(\"添加视图失败：\" + e);\n    }\n});",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "isDisabled": true,
    "id": "A-56d81825-5780-4b0a-844b-67edb657cf4c"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(android.graphics);\nimportPackage(android.util);\nimportPackage(java.lang);\nfunction runOnUiThread(fn) {\n    new Handler(Looper.getMainLooper()).post(new Runnable({ run: fn }));\n}\n\nrunOnUiThread(function() {\n    var windowManager \u003d context.getSystemService(Context.WINDOW_SERVICE);\n\n    var layout \u003d new LinearLayout(context);\n    layout.setOrientation(LinearLayout.VERTICAL);\n    layout.setBackgroundColor(Color.TRANSPARENT);\n\n    var backgroundDrawable \u003d new android.graphics.drawable.GradientDrawable();\n    backgroundDrawable.setColor(Color.WHITE);\n    backgroundDrawable.setCornerRadius(70);\n    layout.setBackground(backgroundDrawable);\n    layout.setPadding(40, 40, 40, 40);\n\n    var layoutParams \u003d new WindowManager.LayoutParams(\n        900,\n        1200,\n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n        WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL | WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,\n        PixelFormat.TRANSLUCENT\n    );\n    layoutParams.gravity \u003d Gravity.CENTER;\n\n    var titleView \u003d new TextView(context);\n    titleView.setText(\"点击选词\");\n    titleView.setTextSize(24);\n    titleView.setTextColor(Color.BLACK);\n    titleView.setGravity(Gravity.CENTER);\n    titleView.setPadding(0, 0, 0, 30);\n    layout.addView(titleView);\n\n    var scrollView \u003d new ScrollView(context);\n    scrollView.setLayoutParams(new LinearLayout.LayoutParams(\n        LinearLayout.LayoutParams.MATCH_PARENT,\n        0, 1\n    ));\n    var wordContainer \u003d new LinearLayout(context);\n    wordContainer.setOrientation(LinearLayout.VERTICAL);\n    scrollView.addView(wordContainer);\n    layout.addView(scrollView);\n\n    var rangeToggleButton \u003d new Button(context);\n    rangeToggleButton.setText(\"范围模式：关闭\");\n    rangeToggleButton.setTextColor(Color.parseColor(\"#000000\"));\n    rangeToggleButton.setBackgroundColor(Color.parseColor(\"#E0E0E0\"));\n    layout.addView(rangeToggleButton);\n\n    var buttonLayout \u003d new LinearLayout(context);\n    buttonLayout.setOrientation(LinearLayout.HORIZONTAL);\n    buttonLayout.setGravity(Gravity.CENTER);\n    buttonLayout.setPadding(0, 20, 0, 0);\n    layout.addView(buttonLayout);\n\n    var confirmButton \u003d new Button(context);\n    confirmButton.setText(\"确定\");\n    confirmButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    confirmButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(confirmButton);\n\n    var cancelButton \u003d new Button(context);\n    cancelButton.setText(\"取消\");\n    cancelButton.setTextColor(Color.parseColor(\"#FF0000\"));\n    cancelButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(cancelButton);\n\n    const inputText \u003d shellOut;\n    const words \u003d inputText ? inputText.match(/[\\u4e00-\\u9fa5]|[a-zA-Z0-9]+|[.,!?;@#]/g) : [];\n    var selectedIndices \u003d [];\n    var rangeMode \u003d false;\n    var rangeStart \u003d -1;\n\n    function getSelectedText() {\n        var selectedWords \u003d [];\n        var sortedIndices \u003d selectedIndices.slice().sort(function(a, b) { return a - b; });\n        sortedIndices.forEach(function(index, i) {\n            selectedWords.push(words[index]);\n        });\n        return selectedWords.reduce(function(result, word, i, arr) {\n            const isCurrentChinese \u003d /[\\u4e00-\\u9fa5]|[。！？；：]/.test(word);\n            const isNextChinese \u003d arr[i + 1] \u0026\u0026 /[\\u4e00-\\u9fa5]|[。！？；：]/.test(arr[i + 1]);\n            result +\u003d word;\n            if (!isCurrentChinese || !isNextChinese) result +\u003d \" \";\n            return result;\n        }, \"\").trim();\n    }\n\n    function toggleSelection(index, view) {\n        if (rangeMode) {\n            if (rangeStart \u003d\u003d\u003d -1) {\n                rangeStart \u003d index;\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            } else {\n                const rangeEnd \u003d index;\n                const min \u003d Math.min(rangeStart, rangeEnd);\n                const max \u003d Math.max(rangeStart, rangeEnd);\n                for (var i \u003d min; i \u003c\u003d max; i++) {\n                    if (!selectedIndices.includes(i)) {\n                        selectedIndices.push(i);\n                        wordElements[i].setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n                    }\n                }\n                rangeStart \u003d -1;\n            }\n        } else {\n            if (selectedIndices.indexOf(index) !\u003d\u003d -1) {\n                selectedIndices \u003d selectedIndices.filter(function(i) { return i !\u003d\u003d index; });\n                view.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n            } else {\n                selectedIndices.push(index);\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            }\n        }\n    }\n\n    var wordElements \u003d [];\n    var totalRowWidthPx \u003d TypedValue.applyDimension(\n        TypedValue.COMPLEX_UNIT_DIP,\n        340,\n        context.getResources().getDisplayMetrics()\n    );\n    var currentRowLayout \u003d new LinearLayout(context);\n    currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n    wordContainer.addView(currentRowLayout);\n    var currentRowWidth \u003d 0;\n\n    var paint \u003d new Paint();\n    var textSizeSp \u003d 20;\n    paint.setTextSize(TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_SP, textSizeSp, context.getResources().getDisplayMetrics()));\n\n    function measureText(text) {\n        return paint.measureText(text);\n    }\n\n    words \u0026\u0026 words.forEach(function(word, index) {\n        var displayWord \u003d word;\n        var measuredWidth \u003d measureText(displayWord) + measureText(\" \") + 20 * 2;\n\n        var textView \u003d new TextView(context);\n        textView.setText(displayWord);\n        textView.setTextSize(textSizeSp);\n        textView.setPadding(10, 10, 10, 10);\n        textView.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n        textView.setGravity(Gravity.START);\n        textView.setOnClickListener(new android.view.View.OnClickListener({\n            onClick: function(view) {\n                toggleSelection(index, view);\n            }\n        }));\n\n        wordElements.push(textView);\n\n        if (measuredWidth \u003e totalRowWidthPx) {\n            // 长单词，强制独占行，并允许两行截断\n            textView.setMaxLines(2);\n            textView.setEllipsize(android.text.TextUtils.TruncateAt.END);\n            textView.setLayoutParams(new LinearLayout.LayoutParams(\n                LinearLayout.LayoutParams.MATCH_PARENT,\n                LinearLayout.LayoutParams.WRAP_CONTENT\n            ));\n\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n            currentRowLayout.addView(textView);\n\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n            currentRowWidth \u003d 0;\n        } else {\n            if (currentRowWidth + measuredWidth \u003e totalRowWidthPx) {\n                // 新行\n                currentRowLayout \u003d new LinearLayout(context);\n                currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n                wordContainer.addView(currentRowLayout);\n                currentRowWidth \u003d 0;\n            }\n\n            currentRowLayout.addView(textView);\n            currentRowWidth +\u003d measuredWidth;\n        }\n    });\n\n    rangeToggleButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            rangeMode \u003d !rangeMode;\n            rangeStart \u003d -1;\n            rangeToggleButton.setText(\"范围模式：\" + (rangeMode ? \"开启\" : \"关闭\"));\n        }\n    }));\n\n    cancelButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\"isclickcancelbutton\")\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    confirmButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var selectedText \u003d getSelectedText();\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\" \"+selectedText)\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    try {\n        windowManager.addView(layout, layoutParams);\n    } catch (e) {\n        console.log(\"添加视图失败：\" + e);\n    }\n});",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "isDisabled": true,
    "id": "A-66fcc60c-d7eb-4f17-bca4-c12337df5cda"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(android.graphics);\nimportPackage(android.util);\nimportPackage(java.lang);\n\nfunction runOnUiThread(fn) {\n    new Handler(Looper.getMainLooper()).post(new Runnable({ run: fn }));\n}\n\nrunOnUiThread(function() {\n    var windowManager \u003d context.getSystemService(Context.WINDOW_SERVICE);\n\n    var layout \u003d new LinearLayout(context);\n    layout.setOrientation(LinearLayout.VERTICAL);\n    layout.setBackgroundColor(Color.TRANSPARENT);\n\n    var backgroundDrawable \u003d new android.graphics.drawable.GradientDrawable();\n    backgroundDrawable.setColor(Color.WHITE);\n    backgroundDrawable.setCornerRadius(70);\n    layout.setBackground(backgroundDrawable);\n    layout.setPadding(40, 40, 40, 40);\n\n    var layoutParams \u003d new WindowManager.LayoutParams(\n        900,\n        1200,\n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n        WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL | WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,\n        PixelFormat.TRANSLUCENT\n    );\n    layoutParams.gravity \u003d Gravity.CENTER;\n\n    var titleView \u003d new TextView(context);\n    titleView.setText(\"点击选词\");\n    titleView.setTextSize(24);\n    titleView.setTextColor(Color.BLACK);\n    titleView.setGravity(Gravity.CENTER);\n    titleView.setPadding(0, 0, 0, 30);\n    layout.addView(titleView);\n\n    var scrollView \u003d new ScrollView(context);\n    scrollView.setLayoutParams(new LinearLayout.LayoutParams(\n        LinearLayout.LayoutParams.MATCH_PARENT,\n        0, 1\n    ));\n    var wordContainer \u003d new LinearLayout(context);\n    wordContainer.setOrientation(LinearLayout.VERTICAL);\n    scrollView.addView(wordContainer);\n    layout.addView(scrollView);\n\n    var buttonLayout \u003d new LinearLayout(context);\n    buttonLayout.setOrientation(LinearLayout.HORIZONTAL);\n    buttonLayout.setGravity(Gravity.CENTER_VERTICAL);\n    buttonLayout.setPadding(0, 20, 0, 0);\n    layout.addView(buttonLayout);\n\n    var rangeToggleButton \u003d new Button(context);\n    rangeToggleButton.setText(\"单选模式\");\n    rangeToggleButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    rangeToggleButton.setBackgroundColor(Color.TRANSPARENT);\n    rangeToggleButton.setPadding(0, 0, 40, 0);  // 与确认按钮留出距离\n    buttonLayout.addView(rangeToggleButton);\n\n    var confirmButton \u003d new Button(context);\n    confirmButton.setText(\"确定\");\n    confirmButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    confirmButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(confirmButton);\n\n    var cancelButton \u003d new Button(context);\n    cancelButton.setText(\"取消\");\n    cancelButton.setTextColor(Color.parseColor(\"#FF0000\"));\n    cancelButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(cancelButton);\n\n    const inputText \u003d shellOut;\n    const words \u003d inputText ? inputText.match(/[\\u4e00-\\u9fa5]|[a-zA-Z0-9]+|[.,!?;@#]/g) : [];\n    var selectedIndices \u003d [];\n    var rangeMode \u003d false;\n    var rangeStart \u003d -1;\n\n    function getSelectedText() {\n        var selectedWords \u003d [];\n        var sortedIndices \u003d selectedIndices.slice().sort(function(a, b) { return a - b; });\n        sortedIndices.forEach(function(index) {\n            selectedWords.push(words[index]);\n        });\n        return selectedWords.reduce(function(result, word, i, arr) {\n            const isCurrentChinese \u003d /[\\u4e00-\\u9fa5]|[。！？；：]/.test(word);\n            const isNextChinese \u003d arr[i + 1] \u0026\u0026 /[\\u4e00-\\u9fa5]|[。！？；：]/.test(arr[i + 1]);\n            result +\u003d word;\n            if (!isCurrentChinese || !isNextChinese) result +\u003d \" \";\n            return result;\n        }, \"\").trim();\n    }\n\n    function toggleSelection(index, view) {\n        if (rangeMode) {\n            if (rangeStart \u003d\u003d\u003d -1) {\n                rangeStart \u003d index;\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            } else {\n                const rangeEnd \u003d index;\n                const min \u003d Math.min(rangeStart, rangeEnd);\n                const max \u003d Math.max(rangeStart, rangeEnd);\n                for (var i \u003d min; i \u003c\u003d max; i++) {\n                    if (!selectedIndices.includes(i)) {\n                        selectedIndices.push(i);\n                        wordElements[i].setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n                    }\n                }\n                rangeStart \u003d -1;\n            }\n        } else {\n            if (selectedIndices.indexOf(index) !\u003d\u003d -1) {\n                selectedIndices \u003d selectedIndices.filter(function(i) { return i !\u003d\u003d index; });\n                view.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n            } else {\n                selectedIndices.push(index);\n                view.setBackgroundColor(Color.parseColor(\"#e0e0e0\"));\n            }\n        }\n    }\n\n    var wordElements \u003d [];\n    var totalRowWidthPx \u003d TypedValue.applyDimension(\n        TypedValue.COMPLEX_UNIT_DIP,\n        340,\n        context.getResources().getDisplayMetrics()\n    );\n    var currentRowLayout \u003d new LinearLayout(context);\n    currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n    wordContainer.addView(currentRowLayout);\n    var currentRowWidth \u003d 0;\n\n    var paint \u003d new Paint();\n    var textSizeSp \u003d 20;\n    paint.setTextSize(TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_SP, textSizeSp, context.getResources().getDisplayMetrics()));\n\n    function measureText(text) {\n        return paint.measureText(text);\n    }\n\n    words \u0026\u0026 words.forEach(function(word, index) {\n        var displayWord \u003d word;\n        var measuredWidth \u003d measureText(displayWord) + measureText(\" \") + 20 * 2;\n\n        var textView \u003d new TextView(context);\n        textView.setText(displayWord);\n        textView.setTextSize(textSizeSp);\n        textView.setPadding(10, 10, 10, 10);\n        textView.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n        textView.setGravity(Gravity.START);\n        textView.setOnClickListener(new android.view.View.OnClickListener({\n            onClick: function(view) {\n                toggleSelection(index, view);\n            }\n        }));\n\n        wordElements.push(textView);\n\n        if (measuredWidth \u003e totalRowWidthPx) {\n            textView.setMaxLines(2);\n            textView.setEllipsize(android.text.TextUtils.TruncateAt.END);\n            textView.setLayoutParams(new LinearLayout.LayoutParams(\n                LinearLayout.LayoutParams.MATCH_PARENT,\n                LinearLayout.LayoutParams.WRAP_CONTENT\n            ));\n\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n            currentRowLayout.addView(textView);\n\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n            currentRowWidth \u003d 0;\n        } else {\n            if (currentRowWidth + measuredWidth \u003e totalRowWidthPx) {\n                currentRowLayout \u003d new LinearLayout(context);\n                currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n                wordContainer.addView(currentRowLayout);\n                currentRowWidth \u003d 0;\n            }\n\n            currentRowLayout.addView(textView);\n            currentRowWidth +\u003d measuredWidth;\n        }\n    });\n\n    rangeToggleButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            rangeMode \u003d !rangeMode;\n            rangeStart \u003d -1;\n            rangeToggleButton.setText(rangeMode ? \"范围模式\" : \"单选模式\");\n        }\n    }));\n\n    cancelButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\"isclickcancelbutton\")\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    confirmButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var selectedText \u003d getSelectedText();\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\" \" + selectedText)\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    try {\n        windowManager.addView(layout, layoutParams);\n    } catch (e) {\n        console.log(\"添加视图失败：\" + e);\n    }\n});",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "isDisabled": true,
    "id": "A-8e3e29cb-53f8-4f79-9f65-f7f71db281e1"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(android.graphics);\nimportPackage(android.util);\nimportPackage(java.lang);\n\nfunction runOnUiThread(fn) {\nnew Handler(Looper.getMainLooper()).post(new Runnable({ run: fn }));\n}\n\nrunOnUiThread(function() {\nvar windowManager \u003d context.getSystemService(Context.WINDOW_SERVICE);\n\nvar layout \u003d new LinearLayout(context);  \nlayout.setOrientation(LinearLayout.VERTICAL);  \nlayout.setBackgroundColor(Color.TRANSPARENT);  \n\nvar backgroundDrawable \u003d new android.graphics.drawable.GradientDrawable();  \nbackgroundDrawable.setColor(Color.WHITE);  \nbackgroundDrawable.setCornerRadius(70);  \nlayout.setBackground(backgroundDrawable);  \nlayout.setPadding(40, 40, 40, 40);  \n\nvar layoutParams \u003d new WindowManager.LayoutParams(  \n    900,  \n    1200,  \n    WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,  \n    WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL | WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,  \n    PixelFormat.TRANSLUCENT  \n);  \nlayoutParams.gravity \u003d Gravity.CENTER;  \n\nvar titleView \u003d new TextView(context);  \ntitleView.setText(\"点击选词\");  \ntitleView.setTextSize(24);  \ntitleView.setTextColor(Color.BLACK);  \ntitleView.setGravity(Gravity.CENTER);  \ntitleView.setPadding(0, 0, 0, 30);  \nlayout.addView(titleView);  \n\nvar scrollView \u003d new ScrollView(context);  \nscrollView.setLayoutParams(new LinearLayout.LayoutParams(  \n    LinearLayout.LayoutParams.MATCH_PARENT,  \n    0, 1  \n));  \nvar wordContainer \u003d new LinearLayout(context);  \nwordContainer.setOrientation(LinearLayout.VERTICAL);  \nscrollView.addView(wordContainer);  \nlayout.addView(scrollView);  \n\nvar buttonLayout \u003d new LinearLayout(context);  \nbuttonLayout.setOrientation(LinearLayout.HORIZONTAL);  \nbuttonLayout.setGravity(Gravity.CENTER);  \nbuttonLayout.setPadding(0, 20, 0, 0);  \nlayout.addView(buttonLayout);  \n\nvar rangeToggleButton \u003d new Button(context);  \nrangeToggleButton.setText(\"单选模式\");  \nrangeToggleButton.setTextColor(Color.parseColor(\"#2D40E9\"));  \nrangeToggleButton.setBackgroundColor(Color.TRANSPARENT);  \nbuttonLayout.addView(rangeToggleButton);  \n\nvar confirmButton \u003d new Button(context);  \nconfirmButton.setText(\"确定\");  \nconfirmButton.setTextColor(Color.parseColor(\"#2D40E9\"));  \nconfirmButton.setBackgroundColor(Color.TRANSPARENT);  \nbuttonLayout.addView(confirmButton);  \n\nvar cancelButton \u003d new Button(context);  \ncancelButton.setText(\"取消\");  \ncancelButton.setTextColor(Color.parseColor(\"#FF0000\"));  \ncancelButton.setBackgroundColor(Color.TRANSPARENT);  \nbuttonLayout.addView(cancelButton);  \n\nconst inputText \u003d shellOut || \"\";  \n\n// 解析文本，拆成数组，包含：  \n// 中文单字、英文单词、数字、标点、空格（空格单独保留）  \nvar tokens \u003d [];  \nvar re \u003d /([\\u4e00-\\u9fa5])|([a-zA-Z0-9]+)|(\\s+)|([^\\w\\s\\u4e00-\\u9fa5])/g;  \nvar match;  \nwhile ((match \u003d re.exec(inputText)) !\u003d\u003d null) {  \n    if (match[1]) { // 单个中文字符  \n        tokens.push({type: \"chinese\", text: match[1]});  \n    } else if (match[2]) { // 英文单词或数字  \n        tokens.push({type: \"word\", text: match[2]});  \n    } else if (match[3]) { // 空格  \n        tokens.push({type: \"space\", text: match[3]});  \n    } else if (match[4]) { // 其他符号标点  \n        tokens.push({type: \"symbol\", text: match[4]});  \n    }  \n}  \n\nvar selectedWordIndices \u003d [];  \nvar rangeMode \u003d false;  \nvar rangeStartIndex \u003d -1;  \n\n// 记录每个单词对应的TextView数组（英文单词每个字母一个TextView）  \nvar wordToTextViews \u003d [];  \n\n// 文字大小和测量相关  \nvar textSizeSp \u003d 20;  \nvar paint \u003d new Paint();  \npaint.setTextSize(TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_SP, textSizeSp, context.getResources().getDisplayMetrics()));  \n\nfunction measureText(text) {  \n    return paint.measureText(text);  \n}  \n\nvar totalRowWidthPx \u003d TypedValue.applyDimension(  \n    TypedValue.COMPLEX_UNIT_DIP,  \n    265,  \n    context.getResources().getDisplayMetrics()  \n);  \n\nvar currentRowLayout \u003d new LinearLayout(context);  \ncurrentRowLayout.setOrientation(LinearLayout.HORIZONTAL);  \nwordContainer.addView(currentRowLayout);  \nvar currentRowWidth \u003d 0;  \n\nfunction updateWordViewBackground(wordIndex, selected) {  \n    var views \u003d wordToTextViews[wordIndex];  \n    if (!views) return;  \n    views.forEach(function(tv) {  \n        tv.setBackgroundColor(selected ? Color.parseColor(\"#e0e0e0\") : Color.parseColor(\"#FAF8FF\"));  \n    });  \n}  \n\nfunction toggleWordSelection(wordIndex) {  \n    var idx \u003d selectedWordIndices.indexOf(wordIndex);  \n    var isSelected \u003d idx !\u003d\u003d -1;  \n    if (isSelected) {  \n        selectedWordIndices.splice(idx, 1);  \n        updateWordViewBackground(wordIndex, false);  \n    } else {  \n        selectedWordIndices.push(wordIndex);  \n        updateWordViewBackground(wordIndex, true);  \n    }  \n}  \n\n// 添加字符控件函数，处理换行  \nfunction addTextViewToRow(textView, width) {  \n    if (currentRowWidth + width \u003e totalRowWidthPx) {  \n        // 换行  \n        currentRowLayout \u003d new LinearLayout(context);  \n        currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);  \n        wordContainer.addView(currentRowLayout);  \n        currentRowWidth \u003d 0;  \n    }  \n    currentRowLayout.addView(textView);  \n    currentRowWidth +\u003d width;  \n}  \n\ntokens.forEach(function(token, wordIndex) {  \n    wordToTextViews[wordIndex] \u003d [];  \n    if (token.type \u003d\u003d\u003d \"space\") {  \n        // 空格用宽固定的TextView显示，不可选中，透明背景，宽度约等于一个字母宽度  \n        var spaceView \u003d new TextView(context);  \n        spaceView.setText(\" \");  \n        spaceView.setTextSize(textSizeSp);  \n        spaceView.setPadding(0, 0, 0, 0);  \n        spaceView.setBackgroundColor(Color.TRANSPARENT);  \n        spaceView.setClickable(false);  \n        spaceView.setFocusable(false);  \n        var spaceWidth \u003d measureText(\" \");  \n        addTextViewToRow(spaceView, spaceWidth);  \n        wordToTextViews[wordIndex].push(spaceView);  \n    } else if (token.type \u003d\u003d\u003d \"chinese\" || token.type \u003d\u003d\u003d \"symbol\") {  \n        // 中文单字或者符号单独一个TextView，支持点击选中  \n        var tv \u003d new TextView(context);  \n        tv.setText(token.text);  \n        tv.setTextSize(textSizeSp);  \n        tv.setPadding(0, 10, 0, 10);  \n        tv.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));  \n        tv.setGravity(Gravity.CENTER);  \n\n        tv.setOnClickListener(new android.view.View.OnClickListener({  \n            onClick: function(view) {  \n                if (rangeMode) {  \n                    if (rangeStartIndex \u003d\u003d\u003d -1) {  \n                        rangeStartIndex \u003d wordIndex;  \n                        toggleWordSelection(wordIndex);  \n                    } else {  \n                        var min \u003d Math.min(rangeStartIndex, wordIndex);  \n                        var max \u003d Math.max(rangeStartIndex, wordIndex);  \n                        for (var i \u003d min; i \u003c\u003d max; i++) {  \n                            if (!selectedWordIndices.includes(i)) {  \n                                selectedWordIndices.push(i);  \n                                updateWordViewBackground(i, true);  \n                            }  \n                        }  \n                        rangeStartIndex \u003d -1;  \n                    }  \n                } else {  \n                    toggleWordSelection(wordIndex);  \n                }  \n            }  \n        }));  \n\n        var w \u003d measureText(token.text);  \n        addTextViewToRow(tv, w);  \n        wordToTextViews[wordIndex].push(tv);  \n    } else if (token.type \u003d\u003d\u003d \"word\") {  \n        // 英文单词按字母拆分，字母无padding且无空隙，整体选中  \n        var letters \u003d token.text.split(\u0027\u0027);  \n        letters.forEach(function(ch) {  \n            var tv \u003d new TextView(context);  \n            tv.setText(ch);  \n            tv.setTextSize(textSizeSp);  \n            tv.setPadding(0, 10, 0, 10);  \n            tv.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));  \n            tv.setGravity(Gravity.CENTER);  \n\n            tv.setOnClickListener(new android.view.View.OnClickListener({  \n                onClick: function(view) {  \n                    if (rangeMode) {  \n                        if (rangeStartIndex \u003d\u003d\u003d -1) {  \n                                rangeStartIndex \u003d wordIndex;  \n                                toggleWordSelection(wordIndex);  \n                            } else {  \n                                var min \u003d Math.min(rangeStartIndex, wordIndex);  \n                                var max \u003d Math.max(rangeStartIndex, wordIndex);  \n                                for (var i \u003d min; i \u003c\u003d max; i++) {  \n                                    if (!selectedWordIndices.includes(i)) {  \n                                        selectedWordIndices.push(i);  \n                                        updateWordViewBackground(i, true);  \n                                    }  \n                                }  \n                                rangeStartIndex \u003d -1;  \n                            }  \n                        } else {  \n                            toggleWordSelection(wordIndex);  \n                        }  \n                    }  \n                }));  \n\n                var letterWidth \u003d measureText(ch);  \n                addTextViewToRow(tv, letterWidth);  \n                wordToTextViews[wordIndex].push(tv);  \n            });  \n        }  \n    });\n\nfunction getSelectedText() {\nselectedWordIndices.sort(function(a, b) { return a - b; });\n\nvar result \u003d \"\";  \nvar lastType \u003d null;  \n\nselectedWordIndices.forEach(function(i) {  \n    var token \u003d tokens[i];  \n    var currentType \u003d token.type;  \n\n    // 中文-中文不加空格，其余组合之间加空格  \n    if (result.length \u003e 0 \u0026\u0026  \n        !(  \n            (lastType \u003d\u003d\u003d \"chinese\" \u0026\u0026 currentType \u003d\u003d\u003d \"chinese\")  \n        )) {  \n        result +\u003d \" \";  \n    }  \n\n    result +\u003d token.text;  \n    lastType \u003d currentType;  \n});  \n\nreturn result;\n\n}\nrangeToggleButton.setOnClickListener(new android.view.View.OnClickListener({\nonClick: function(view) {\nrangeMode \u003d !rangeMode;\nrangeStartIndex \u003d -1;\nrangeToggleButton.setText(rangeMode ? \"范围模式\" : \"单选模式\");\n}\n}));\n\nconfirmButton.setOnClickListener(new android.view.View.OnClickListener({  \n    onClick: function(view) {  \n        var selectedText \u003d getSelectedText();  \n        var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()  \n            .setVarName(\"selectedText\")  \n            .setValueAsString(\" \" + selectedText)  \n            .build();  \n        shortx.executeAction(writeLocalVar);  \n        try {  \n            windowManager.removeView(layout);  \n        } catch (e) {}  \n    }  \n}));  \n\ncancelButton.setOnClickListener(new android.view.View.OnClickListener({  \n    onClick: function(view) {  \n        var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()  \n            .setVarName(\"selectedText\")  \n            .setValueAsString(\"isclickcancelbutton\")  \n            .build();  \n        shortx.executeAction(writeLocalVar);  \n        try {  \n            windowManager.removeView(layout);  \n        } catch (e) {}  \n    }  \n}));  \n\ntry {  \n    windowManager.addView(layout, layoutParams);  \n} catch (e) {  \n    console.log(\"添加视图失败：\" + e);  \n}\n\n});\n\n\n",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "isDisabled": true,
    "id": "A-673c08e3-51b5-403c-b0df-8b70204ddec4"
  }, {
    "@type": "type.googleapis.com/ExecuteJS",
    "expression": "importPackage(android.view);\nimportPackage(android.widget);\nimportPackage(android.content);\nimportPackage(android.os);\nimportPackage(android.graphics);\nimportPackage(android.util);\nimportPackage(java.lang);\n\nfunction runOnUiThread(fn) {\n    new Handler(Looper.getMainLooper()).post(new Runnable({ run: fn }));\n}\n\nrunOnUiThread(function() {\n    var windowManager \u003d context.getSystemService(Context.WINDOW_SERVICE);\n\n    var layout \u003d new LinearLayout(context);\n    layout.setOrientation(LinearLayout.VERTICAL);\n    layout.setBackgroundColor(Color.TRANSPARENT);\n\n    var backgroundDrawable \u003d new android.graphics.drawable.GradientDrawable();\n    backgroundDrawable.setColor(Color.WHITE);\n    backgroundDrawable.setCornerRadius(70);\n    layout.setBackground(backgroundDrawable);\n    layout.setPadding(40, 40, 40, 40);\n\n    var layoutParams \u003d new WindowManager.LayoutParams(\n        900,\n        1200,\n        WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,\n        WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL | WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,\n        PixelFormat.TRANSLUCENT\n    );\n    layoutParams.gravity \u003d Gravity.CENTER;\n\n    var titleView \u003d new TextView(context);\n    titleView.setText(\"点击选词\");\n    titleView.setTextSize(24);\n    titleView.setTextColor(Color.BLACK);\n    titleView.setGravity(Gravity.CENTER);\n    titleView.setPadding(0, 0, 0, 30);\n    layout.addView(titleView);\n\n    var scrollView \u003d new ScrollView(context);\n    scrollView.setLayoutParams(new LinearLayout.LayoutParams(\n        LinearLayout.LayoutParams.MATCH_PARENT,\n        0, 1\n    ));\n    var wordContainer \u003d new LinearLayout(context);\n    wordContainer.setOrientation(LinearLayout.VERTICAL);\n    scrollView.addView(wordContainer);\n    layout.addView(scrollView);\n\n    var buttonLayout \u003d new LinearLayout(context);\n    buttonLayout.setOrientation(LinearLayout.HORIZONTAL);\n    buttonLayout.setGravity(Gravity.CENTER);\n    buttonLayout.setPadding(0, 20, 0, 0);\n    layout.addView(buttonLayout);\n\n    var rangeToggleButton \u003d new Button(context);\n    rangeToggleButton.setText(\"单选模式\");\n    rangeToggleButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    rangeToggleButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(rangeToggleButton);\n\n    var confirmButton \u003d new Button(context);\n    var cancelButton \u003d new Button(context);\n    cancelButton.setText(\"取消\");\n    cancelButton.setTextColor(Color.parseColor(\"#FF0000\"));\n    cancelButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(cancelButton);\n    \n    confirmButton.setText(\"确定\");\n    confirmButton.setTextColor(Color.parseColor(\"#2D40E9\"));\n    confirmButton.setBackgroundColor(Color.TRANSPARENT);\n    buttonLayout.addView(confirmButton);\n\n    const inputText \u003d shellOut || \"\";\n\n    // 支持中文、英文单词、空格、标点、换行\n    var tokens \u003d [];\n    var re \u003d /([\\u4e00-\\u9fa5])|([a-zA-Z0-9]+)|(\\r?\\n)|(\\s+)|([^\\w\\s\\u4e00-\\u9fa5])/g;\n    var match;\n    while ((match \u003d re.exec(inputText)) !\u003d\u003d null) {\n        if (match[1]) {\n            tokens.push({ type: \"chinese\", text: match[1] });\n        } else if (match[2]) {\n            tokens.push({ type: \"word\", text: match[2] });\n        } else if (match[3]) {\n            tokens.push({ type: \"newline\", text: \"\\n\" });\n        } else if (match[4]) {\n            tokens.push({ type: \"space\", text: match[4] });\n        } else if (match[5]) {\n            tokens.push({ type: \"symbol\", text: match[5] });\n        }\n    }\n\n    var selectedWordIndices \u003d [];\n    var rangeMode \u003d false;\n    var rangeStartIndex \u003d -1;\n    var wordToTextViews \u003d [];\n\n    var textSizeSp \u003d 20;\n    var paint \u003d new Paint();\n    paint.setTextSize(TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_SP, textSizeSp, context.getResources().getDisplayMetrics()));\n\n    function measureText(text) {\n        return paint.measureText(text);\n    }\n\n    var totalRowWidthPx \u003d TypedValue.applyDimension(\n        TypedValue.COMPLEX_UNIT_DIP,\n        265,\n        context.getResources().getDisplayMetrics()\n    );\n\n    var currentRowLayout \u003d new LinearLayout(context);\n    currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n    wordContainer.addView(currentRowLayout);\n    var currentRowWidth \u003d 0;\n\n    function updateWordViewBackground(wordIndex, selected) {\n        var views \u003d wordToTextViews[wordIndex];\n        if (!views) return;\n        views.forEach(function(tv) {\n            tv.setBackgroundColor(selected ? Color.parseColor(\"#e0e0e0\") : Color.parseColor(\"#FAF8FF\"));\n        });\n    }\n\n    function toggleWordSelection(wordIndex) {\n        var idx \u003d selectedWordIndices.indexOf(wordIndex);\n        var isSelected \u003d idx !\u003d\u003d -1;\n        if (isSelected) {\n            selectedWordIndices.splice(idx, 1);\n            updateWordViewBackground(wordIndex, false);\n        } else {\n            selectedWordIndices.push(wordIndex);\n            updateWordViewBackground(wordIndex, true);\n        }\n    }\n\n    function addTextViewToRow(textView, width) {\n        if (currentRowWidth + width \u003e totalRowWidthPx) {\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n            currentRowWidth \u003d 0;\n        }\n        currentRowLayout.addView(textView);\n        currentRowWidth +\u003d width;\n    }\n\n    tokens.forEach(function(token, wordIndex) {\n        wordToTextViews[wordIndex] \u003d [];\n\n        if (token.type \u003d\u003d\u003d \"newline\") {\n            currentRowLayout \u003d new LinearLayout(context);\n            currentRowLayout.setOrientation(LinearLayout.HORIZONTAL);\n            wordContainer.addView(currentRowLayout);\n            currentRowWidth \u003d 0;\n            return;\n        }\n\n        if (token.type \u003d\u003d\u003d \"space\") {\n            var spaceView \u003d new TextView(context);\n            spaceView.setText(\" \");\n            spaceView.setTextSize(textSizeSp);\n            spaceView.setPadding(0, 0, 0, 0);\n            spaceView.setBackgroundColor(Color.TRANSPARENT);\n            spaceView.setClickable(false);\n            spaceView.setFocusable(false);\n            var spaceWidth \u003d measureText(\" \");\n            addTextViewToRow(spaceView, spaceWidth);\n            wordToTextViews[wordIndex].push(spaceView);\n        } else if (token.type \u003d\u003d\u003d \"chinese\" || token.type \u003d\u003d\u003d \"symbol\") {\n            var tv \u003d new TextView(context);\n            tv.setText(token.text);\n            tv.setTextSize(textSizeSp);\n            tv.setPadding(0, 10, 0, 10);\n            tv.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n            tv.setGravity(Gravity.CENTER);\n            tv.setOnClickListener(new android.view.View.OnClickListener({\n                onClick: function(view) {\n                    if (rangeMode) {\n                        if (rangeStartIndex \u003d\u003d\u003d -1) {\n                            rangeStartIndex \u003d wordIndex;\n                            toggleWordSelection(wordIndex);\n                        } else {\n                            var min \u003d Math.min(rangeStartIndex, wordIndex);\n                            var max \u003d Math.max(rangeStartIndex, wordIndex);\n                            for (var i \u003d min; i \u003c\u003d max; i++) {\n                                if (!selectedWordIndices.includes(i)) {\n                                    selectedWordIndices.push(i);\n                                    updateWordViewBackground(i, true);\n                                }\n                            }\n                            rangeStartIndex \u003d -1;\n                        }\n                    } else {\n                        toggleWordSelection(wordIndex);\n                    }\n                }\n            }));\n            var w \u003d measureText(token.text);\n            addTextViewToRow(tv, w);\n            wordToTextViews[wordIndex].push(tv);\n        } else if (token.type \u003d\u003d\u003d \"word\") {\n            var letters \u003d token.text.split(\u0027\u0027);\n            letters.forEach(function(ch) {\n                var tv \u003d new TextView(context);\n                tv.setText(ch);\n                tv.setTextSize(textSizeSp);\n                tv.setPadding(0, 10, 0, 10);\n                tv.setBackgroundColor(Color.parseColor(\"#FAF8FF\"));\n                tv.setGravity(Gravity.CENTER);\n                tv.setOnClickListener(new android.view.View.OnClickListener({\n                    onClick: function(view) {\n                        if (rangeMode) {\n                            if (rangeStartIndex \u003d\u003d\u003d -1) {\n                                rangeStartIndex \u003d wordIndex;\n                                toggleWordSelection(wordIndex);\n                            } else {\n                                var min \u003d Math.min(rangeStartIndex, wordIndex);\n                                var max \u003d Math.max(rangeStartIndex, wordIndex);\n                                for (var i \u003d min; i \u003c\u003d max; i++) {\n                                    if (!selectedWordIndices.includes(i)) {\n                                        selectedWordIndices.push(i);\n                                        updateWordViewBackground(i, true);\n                                    }\n                                }\n                                rangeStartIndex \u003d -1;\n                            }\n                        } else {\n                            toggleWordSelection(wordIndex);\n                        }\n                    }\n                }));\n                var letterWidth \u003d measureText(ch);\n                addTextViewToRow(tv, letterWidth);\n                wordToTextViews[wordIndex].push(tv);\n            });\n        }\n    });\n\n    function getSelectedText() {\n        selectedWordIndices.sort(function(a, b) { return a - b; });\n\n        var result \u003d \"\";\n        var lastType \u003d null;\n\n        selectedWordIndices.forEach(function(i) {\n            var token \u003d tokens[i];\n            var currentType \u003d token.type;\n            if (result.length \u003e 0 \u0026\u0026 !(lastType \u003d\u003d\u003d \"chinese\" \u0026\u0026 currentType \u003d\u003d\u003d \"chinese\")) {\n                result +\u003d \" \";\n            }\n            result +\u003d token.text;\n            lastType \u003d currentType;\n        });\n\n        return result;\n    }\n\n    rangeToggleButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            rangeMode \u003d !rangeMode;\n            rangeStartIndex \u003d -1;\n            rangeToggleButton.setText(rangeMode ? \"范围模式\" : \"单选模式\");\n        }\n    }));\n\n    confirmButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var selectedText \u003d getSelectedText();\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\" \" + selectedText)\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    cancelButton.setOnClickListener(new android.view.View.OnClickListener({\n        onClick: function(view) {\n            var writeLocalVar \u003d Packages.tornaco.apps.shortx.core.proto.action.WriteLocalVar.newBuilder()\n                .setVarName(\"selectedText\")\n                .setValueAsString(\"isclickcancelbutton\")\n                .build();\n            shortx.executeAction(writeLocalVar);\n            try {\n                windowManager.removeView(layout);\n            } catch (e) {}\n        }\n    }));\n\n    try {\n        windowManager.addView(layout, layoutParams);\n    } catch (e) {\n        console.log(\"添加视图失败：\" + e);\n    }\n});",
    "context": "CoroutineContext_UI",
    "customContextDataKey": {
    },
    "id": "A-64d5ec1a-d27f-43c2-9c44-36cd3c6568d1"
  }, {
    "@type": "type.googleapis.com/WaitUtilConditionMatch",
    "condition": [{
      "@type": "type.googleapis.com/EvaluateLocalVar",
      "op": "IsNotEmpty",
      "varName": "selectedText",
      "payload": {
      },
      "customContextDataKey": {
      },
      "id": "C-f43fc5a6-727a-4ed1-9906-c64bffcae034"
    }],
    "quitFacts": [{
      "@type": "type.googleapis.com/Broadcast",
      "actions": ["dbz.nyehueh.SELECTED_TEXT_cancel"],
      "customContextDataKey": {
      },
      "id": "F-13d57b1f-e22c-4fdb-b117-81cef85027b5"
    }],
    "quitEnabled": true,
    "timeoutStr": "0",
    "customContextDataKey": {
    },
    "id": "A-ec45daa9-e646-4c9e-b795-3f24fc4ee699"
  }, {
    "@type": "type.googleapis.com/IfThenElse",
    "If": [{
      "@type": "type.googleapis.com/EvaluateLocalVar",
      "op": "EqualTo",
      "varName": "selectedText",
      "payload": {
        "value": "isclickcancelbutton"
      },
      "customContextDataKey": {
      },
      "id": "C-98aee813-5c6d-49a9-a630-bf6cecfab594"
    }],
    "ElseActions": [{
      "@type": "type.googleapis.com/ShowAlertDialog",
      "positive": "复制",
      "onPositive": [{
        "@type": "type.googleapis.com/WriteClipboard",
        "text": "localVarOf$selectedText",
        "customContextDataKey": {
        },
        "id": "A-51800d3d-5ddc-47bf-8cdc-dbfa4d84dcd9"
      }],
      "negative": "取消",
      "title": "选择文本",
      "message": "localVarOf$selectedText",
      "cancelable": true,
      "style": {
      },
      "customContextDataKey": {
      },
      "id": "A-be7f5e3e-9f9a-4619-8d40-fd6175815124"
    }],
    "customContextDataKey": {
    },
    "id": "A-10ecb80d-9a87-4fee-9c96-66f545b38a2d"
  }],
  "id": "SHARED-DA-4f755cfe-f98f-4a22-bccc-ae07abd71399",
  "lastUpdateTime": "1748449639809",
  "createTime": "1730728682467",
  "author": {
    "name": "nyehueh"
  },
  "title": "大爆炸",
  "versionCode": "1",
  "quit": {
    "isEnabled": true
  }
}
###------###
{"type":"da"}